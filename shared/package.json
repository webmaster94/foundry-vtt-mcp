{
  "name": "@foundry-mcp/shared",
  "version": "0.13.0",
  "description": "Shared TypeScript types and constants for Foundry MCP Integration",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "node ../scripts/clean-build.mjs shared",
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "files": [
    "dist"
  ]
}
